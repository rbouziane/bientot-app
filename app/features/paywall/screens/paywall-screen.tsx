import { memo } from 'react';
import PlaceholderScreen from '~shared/components/PlaceholderScreen';

const PaywallScreen = memo(() => {
  return <PlaceholderScreen title="Paywall" />;
});

export default PaywallScreen;
