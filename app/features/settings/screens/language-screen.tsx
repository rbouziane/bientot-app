import { memo } from 'react';
import PlaceholderScreen from '~shared/components/PlaceholderScreen';

const LanguageScreen = memo(() => {
  return <PlaceholderScreen title="Language" />;
});

export default LanguageScreen;
