import { memo } from 'react';
import PlaceholderScreen from '~shared/components/PlaceholderScreen';

const EventDetailScreen = memo(() => {
  return <PlaceholderScreen title="Event detail" />;
});

export default EventDetailScreen;
