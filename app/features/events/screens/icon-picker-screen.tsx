import { memo } from 'react';

import PlaceholderScreen from '~shared/components/PlaceholderScreen';

const IconPickerScreen = memo(() => {
  return <PlaceholderScreen title="Icon picker" />;
});

export default IconPickerScreen;
