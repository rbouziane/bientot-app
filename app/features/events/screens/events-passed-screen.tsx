import { memo } from 'react';
import PlaceholderScreen from '~shared/components/PlaceholderScreen';

const EventsPassedScreen = memo(() => {
  return <PlaceholderScreen title="Passed events" />;
});

export default EventsPassedScreen;
