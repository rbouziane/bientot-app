import { memo } from 'react';
import PlaceholderScreen from '~shared/components/PlaceholderScreen';

const ShareCardScreen = memo(() => {
  return <PlaceholderScreen title="Share card" />;
});

export default ShareCardScreen;
