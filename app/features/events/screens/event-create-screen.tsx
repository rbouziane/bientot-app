import { memo } from 'react';
import PlaceholderScreen from '~shared/components/PlaceholderScreen';

const EventCreateScreen = memo(() => {
  return <PlaceholderScreen title="Create event" />;
});

export default EventCreateScreen;
