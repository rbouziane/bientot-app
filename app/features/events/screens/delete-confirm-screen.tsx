import { memo } from 'react';
import PlaceholderScreen from '~shared/components/PlaceholderScreen';

const DeleteConfirmScreen = memo(() => {
  return <PlaceholderScreen title="Delete confirm" />;
});

export default DeleteConfirmScreen;
