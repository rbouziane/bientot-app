import { memo } from 'react';
import PlaceholderScreen from '~shared/components/PlaceholderScreen';

const ColorPickerScreen = memo(() => {
  return <PlaceholderScreen title="Color picker" />;
});

export default ColorPickerScreen;
